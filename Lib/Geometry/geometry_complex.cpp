#include<bits/stdc++.h>

// abs(p) -> absolute ;  arg(p) -> argument in [-pi, pi]
typedef double T;
typedef complex<T> pt;
#define x real()
#define y imag()

pt rot(pt p, double a) {return p * polar(1.0, a);}


int main(){
	
	return 0;
}