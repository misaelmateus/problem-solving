//   Author : Misael Mateus 
//   Submission date: 2017-05-20 23:40:34
#include<bits/stdc++.h>
#define ll long long
#define ld long double
#define EPS 0.000000001
#define fr(a, b, c) for(int (a) = (b); (a) < (c); (a)++);
#define rep(a, b) fr(a, 0, c)
#define fill(v, x) memset(v, x, sizeof(v))
#define RI(x) scanf("%d", &x)
#define RII(x, y) scanf("%d %d", &x, &y)


using namespace std;

int main(){
	int n, k, aux;
	cin >> n >> k;
	int sum = 0;
	for(int i = 0; i < n; i++){
		cin >> aux;
		sum += aux;
	}
	int x = 2*k*n - n - 2*sum;
	if(x < 0)
		x = 0;
	cout << x << endl;

}#include<bits/stdc++.h>
#define ll long long
#define ld long double
#define EPS 0.000000001
#define fr(a, b, c) for(int (a) = (b); (a) < (c); (a)++);
#define rep(a, b) fr(a, 0, c)
#define fill(v, x) memset(v, x, sizeof(v))
#define RI(x) scanf("%d", &x)
#define RII(x, y) scanf("%d %d", &x, &y)


using namespace std;

int main(){
	int n, k, aux;
	cin >> n >> k;
	int sum = 0;
	for(int i = 0; i < n; i++){
		cin >> aux;
		sum += aux;
	}
	int x = 2*k*n - n - 2*sum;
	if(x < 0)
		x = 0;
	cout << x << endl;

}#include<bits/stdc++.h>
#define ll long long
#define ld long double
#define EPS 0.000000001
#define fr(a, b, c) for(int (a) = (b); (a) < (c); (a)++);
#define rep(a, b) fr(a, 0, c)
#define fill(v, x) memset(v, x, sizeof(v))
#define RI(x) scanf("%d", &x)
#define RII(x, y) scanf("%d %d", &x, &y)


using namespace std;

int main(){
	int n, k, aux;
	cin >> n >> k;
	int sum = 0;
	for(int i = 0; i < n; i++){
		cin >> aux;
		sum += aux;
	}
	int x = 2*k*n - n - 2*sum;
	if(x < 0)
		x = 0;
	cout << x << endl;

}#include<bits/stdc++.h>
#define ll long long
#define ld long double
#define EPS 0.000000001
#define fr(a, b, c) for(int (a) = (b); (a) < (c); (a)++);
#define rep(a, b) fr(a, 0, c)
#define fill(v, x) memset(v, x, sizeof(v))
#define RI(x) scanf("%d", &x)
#define RII(x, y) scanf("%d %d", &x, &y)


using namespace std;

int main(){
	int n, k, aux;
	cin >> n >> k;
	int sum = 0;
	for(int i = 0; i < n; i++){
		cin >> aux;
		sum += aux;
	}
	int x = 2*k*n - n - 2*sum;
	if(x < 0)
		x = 0;
	cout << x << endl;

}