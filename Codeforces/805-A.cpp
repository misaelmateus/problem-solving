//   Author : Misael Mateus 
//   Submission date: 2017-05-04 20:24:38
#include<bits/stdc++.h>
#define fr(a, b, c) for(int a = (b); (b) < (c); c++)
#define rep(a, b) fr(a, 0, c)
#define ll long long
#define ld long double
#define fill(v, x) memset(v, x, sizeof(v))
using namespace std;
int main(){
	int l, r;
	cin >> l >> r;
	if(l == r)
		cout << l << "\n";
	else cout << 2 << "\n";

	return 0;
}#include<bits/stdc++.h>
#define fr(a, b, c) for(int a = (b); (b) < (c); c++)
#define rep(a, b) fr(a, 0, c)
#define ll long long
#define ld long double
#define fill(v, x) memset(v, x, sizeof(v))
using namespace std;
int main(){
	int l, r;
	cin >> l >> r;
	if(l == r)
		cout << l << "\n";
	else cout << 2 << "\n";

	return 0;
}#include<bits/stdc++.h>
#define fr(a, b, c) for(int a = (b); (b) < (c); c++)
#define rep(a, b) fr(a, 0, c)
#define ll long long
#define ld long double
#define fill(v, x) memset(v, x, sizeof(v))
using namespace std;
int main(){
	int l, r;
	cin >> l >> r;
	if(l == r)
		cout << l << "\n";
	else cout << 2 << "\n";

	return 0;
}#include<bits/stdc++.h>
#define fr(a, b, c) for(int a = (b); (b) < (c); c++)
#define rep(a, b) fr(a, 0, c)
#define ll long long
#define ld long double
#define fill(v, x) memset(v, x, sizeof(v))
using namespace std;
int main(){
	int l, r;
	cin >> l >> r;
	if(l == r)
		cout << l << "\n";
	else cout << 2 << "\n";

	return 0;
}