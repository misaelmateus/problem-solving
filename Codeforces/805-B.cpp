//   Author : Misael Mateus 
//   Submission date: 2017-05-04 20:27:47
#include<bits/stdc++.h>
#define fr(a, b, c) for(int a = (b); (b) < (c); c++)
#define rep(a, b) fr(a, 0, c)
#define ll long long
#define ld long double
#define fill(v, x) memset(v, x, sizeof(v))
using namespace std;
int main(){
	ios_base::sync_with_stdio(false);
	int n;
	cin >> n;
	string s = "";
	string a[] = {"aa", "bb"};
	int i = 0;
	while(n > 1){
		s += a[i];
		i ^= 1;
		n -= 2;
	}
	if(n) s += (i == 0 ? 'a' : 'b');

	cout << s << "\n";

	return 0;
}#include<bits/stdc++.h>
#define fr(a, b, c) for(int a = (b); (b) < (c); c++)
#define rep(a, b) fr(a, 0, c)
#define ll long long
#define ld long double
#define fill(v, x) memset(v, x, sizeof(v))
using namespace std;
int main(){
	ios_base::sync_with_stdio(false);
	int n;
	cin >> n;
	string s = "";
	string a[] = {"aa", "bb"};
	int i = 0;
	while(n > 1){
		s += a[i];
		i ^= 1;
		n -= 2;
	}
	if(n) s += (i == 0 ? 'a' : 'b');

	cout << s << "\n";

	return 0;
}#include<bits/stdc++.h>
#define fr(a, b, c) for(int a = (b); (b) < (c); c++)
#define rep(a, b) fr(a, 0, c)
#define ll long long
#define ld long double
#define fill(v, x) memset(v, x, sizeof(v))
using namespace std;
int main(){
	ios_base::sync_with_stdio(false);
	int n;
	cin >> n;
	string s = "";
	string a[] = {"aa", "bb"};
	int i = 0;
	while(n > 1){
		s += a[i];
		i ^= 1;
		n -= 2;
	}
	if(n) s += (i == 0 ? 'a' : 'b');

	cout << s << "\n";

	return 0;
}#include<bits/stdc++.h>
#define fr(a, b, c) for(int a = (b); (b) < (c); c++)
#define rep(a, b) fr(a, 0, c)
#define ll long long
#define ld long double
#define fill(v, x) memset(v, x, sizeof(v))
using namespace std;
int main(){
	ios_base::sync_with_stdio(false);
	int n;
	cin >> n;
	string s = "";
	string a[] = {"aa", "bb"};
	int i = 0;
	while(n > 1){
		s += a[i];
		i ^= 1;
		n -= 2;
	}
	if(n) s += (i == 0 ? 'a' : 'b');

	cout << s << "\n";

	return 0;
}