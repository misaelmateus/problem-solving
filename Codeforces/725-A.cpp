//   Author : Misael Mateus 
//   Submission date: 2016-10-22 20:42:00
#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    string s;
    cin >> n >> s;
    
    int cont = 0;
    for(int i = 0; i < n && s[i] == '<'; i++, cont++);
    for(int i = n-1; i >= 0 && s[i] =='>'; i--, cont++);
    
    cout << cont << endl;
    
    return 0;
}#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    string s;
    cin >> n >> s;
    
    int cont = 0;
    for(int i = 0; i < n && s[i] == '<'; i++, cont++);
    for(int i = n-1; i >= 0 && s[i] =='>'; i--, cont++);
    
    cout << cont << endl;
    
    return 0;
}#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    string s;
    cin >> n >> s;
    
    int cont = 0;
    for(int i = 0; i < n && s[i] == '<'; i++, cont++);
    for(int i = n-1; i >= 0 && s[i] =='>'; i--, cont++);
    
    cout << cont << endl;
    
    return 0;
}#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    string s;
    cin >> n >> s;
    
    int cont = 0;
    for(int i = 0; i < n && s[i] == '<'; i++, cont++);
    for(int i = n-1; i >= 0 && s[i] =='>'; i--, cont++);
    
    cout << cont << endl;
    
    return 0;
}