//   Author : Misael Mateus 
//   Submission date: 2017-05-04 20:29:08
#include<bits/stdc++.h>
#define fr(a, b, c) for(int a = (b); (b) < (c); c++)
#define rep(a, b) fr(a, 0, c)
#define ll long long
#define ld long double
#define fill(v, x) memset(v, x, sizeof(v))
using namespace std;
int main(){
	ios_base::sync_with_stdio(false);
	int n;
	cin >> n;
	cout << (int)((n+1)/2 - 1) << "\n";

	return 0;
}